CONFIG_GPIO=y
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=3
CONFIG_UART_CONSOLE=y

# 系统tick配置
CONFIG_SYS_CLOCK_TICKS_PER_SEC=1000

# Flash support
CONFIG_FLASH=y

# 使用Flash模拟器（native_sim不需要真实SPI硬件）
CONFIG_FLASH_SIMULATOR=y